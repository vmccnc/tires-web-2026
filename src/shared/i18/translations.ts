import { en, pl, ru } from '@/shared/i18/locales';

import type { Language, TranslationNode } from '@/shared/i18/model';

export const translations: Record<Language, TranslationNode> = {
  en,
  pl,
  ru,
};
